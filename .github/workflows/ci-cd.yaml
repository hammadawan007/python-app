name: Build and Deploy Flask App

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Log in to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${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 }

    - name: Build Docker image
      run: |
        docker build -t ghcr.io/${hammadawan007/python-app}:latest .

    - name: Push image to GHCR
      run: |
        docker push ghcr.io/${hammadawan007/python-app}:latest

